import { HeaderContainer } from "./styles";

const Header = () => {
  return (
    <HeaderContainer>
      <p>Calculadora</p>
      <button>Menu</button>
    </HeaderContainer>
  );
};

export default Header;
